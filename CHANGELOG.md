# Changelog

## [1.1.0] - 2026-07-16

### 追加

- **listコマンド**（エイリアス: `ls` / `status`）を追加
  - 実行中タスク・stash・doneアーカイブの一覧と、各タスクのサイクル状態（現在サイクル番号、次のアクションがrun/reviewか、plan/review数）を表示
  - `--json` でAIエージェント向けのJSON出力に対応（タスクディレクトリ未作成でもエラーにならず空の結果を返す）
  - `--all` でdoneアーカイブ内のタスク名まで表示
- **planテンプレートのカスタマイズ**に対応
  - `package.json` の `plsr-task.template` にテンプレートファイルのパスを指定すると、`create` のplan雛形を差し替えられる
  - プレースホルダ `{{taskName}}`（タスク名）と `{{date}}`（作成日 yyyy-MM-dd）に対応。未知のプレースホルダはそのまま残る
  - テンプレートが存在しない・読めない・空の場合は警告を表示して内蔵デフォルトにフォールバック（createは失敗しない）
- ドキュメントを追加: [コマンドリファレンス](docs/commands.md) / [設定ガイド](docs/configuration.md)

### 変更

- **create**: 実行中タスクがある場合、既存タスクをstashへ強制退避するのをやめ、新タスクを `stash/<タスク名>/` に作成するように変更（実行中の作業を邪魔しないため）。取り出すときは `pop -t <タスク名>` を使う
- **pop**: タスク名未指定のとき、stashが1件だけでも自動選択せず必ずリスト選択を挟むように変更（誤popの防止）。スクリプトからの非対話実行では `-t` を指定すること
- `--version` の表示をpackage.jsonから動的に取得するように変更（ハードコード解消）

### 修正

- **pop**: リスト選択が表示されず直接入力になってしまう問題を修正（inquirer v10以降で `list` タイプが `select` に改名されたことが原因）

### セキュリティ

- `package.json` に `files` を指定し、npmパッケージの公開内容を `dist/` のみに限定（タスクファイル等が誤って公開されるのを防止）

## [1.0.1] - 以前

- guideコマンド追加（AIエージェント向けセットアップガイド、`--install` でtask-cycleスキルを自動インストール）
- guideで参加者名を指定するとループが発生する問題を修正

## [1.0.0]

- 初回リリース（create / cycle / stash / pop / done）
